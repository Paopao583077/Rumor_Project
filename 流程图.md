# 毕设项目：基于多模态深度学习的社交媒体谣言监测系统

## 1. 系统全局流程设计

本系统主要包含三个阶段：数据预处理、模型构建与训练、谣言预测。下图展示了数据在系统中的流转过程。

```mermaid
graph TD
    classDef data fill:#e1f5fe,stroke:#01579b,stroke-width:2px;
    classDef process fill:#fff9c4,stroke:#fbc02d,stroke-width:2px;
    classDef model fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px;

    A1[原始图片 data/images]:::data --> P1(preprocess.py):::process
    A2[原始文本 data/tweets]:::data --> P1
    P1 --> D1[CSV 索引文件]:::data
    
    D1 --> P2(train.py):::process
    P2 --> M1{{多模态模型训练}}:::model
    M1 --> OUT[best_model.pth]:::data
    
    OUT --> P3(predict.py):::process
    USER[用户输入] --> P3
    P3 --> RES((预测结果)):::model
```

## 2. 模型核心架构

本研究采用了 **BERT + ResNet** 的双塔结构，并设计了 **文本引导的注意力机制 (Text-Guided Attention)** 来解决图文语义不对齐的问题。

* **文本分支**：使用预训练的 BERT 提取深层语义特征。
* **视觉分支**：使用 ResNet-50 提取图像的空间特征。
* **融合模块**：利用文本向量作为“查询（Query）”，动态计算图像不同区域的权重，从而让模型关注与文本描述最相关的视觉区域。

```mermaid
graph LR
    classDef encoder fill:#e3f2fd,stroke:#1565c0,stroke-width:2px;
    classDef fusion fill:#fff3e0,stroke:#ef6c00,stroke-width:2px;
    classDef out fill:#ffebee,stroke:#c62828,stroke-width:2px;

    InT[文本] --> BERT[BERT]:::encoder
    InI[图片] --> CNN[ResNet]:::encoder
    
    BERT --> T_Feat[文本特征]
    CNN --> I_Feat[视觉特征图]
    
    T_Feat --> Attn{注意力融合}:::fusion
    I_Feat --> Attn
    
    Attn --> V_Feat[加权视觉特征]
    
    T_Feat --> Concat[拼接]
    V_Feat --> Concat
    
    Concat --> FC[分类器]:::out
    FC --> Out((谣言概率)):::out
```



A[启动程序] --> B[加载 BERT & ResNet 权重];
B --> C{等待用户操作};

C -->|输入文本 + 上传图片| D[点击 '开始分析'];

D --> E[数据预处理];
E --> F[图片转 224x224 Tensor];
E --> G[文本转 Token IDs];

F --> H[模型推理 (forward)];
G --> H;

H --> I[BERT提取文本特征];
H --> J[ResNet提取视觉特征];

I --> K[Attention 融合层];
J --> K;

K --> L[全连接分类器];
L --> M[计算 Softmax 概率];

M --> N[界面更新: 显示红/绿结果];
N --> C;